repositories {
    mavenLocal()
    maven {
        url 'http://localhost:8081/nexus/content/groups/public/'
    }
}

dependencies {
    compile gradleApi()
    groovy localGroovy()
    compile(
            ['com.github.signed.gradle.plugin:hamcrest-generator-source:0.0.1-SNAPSHOT']
    )
}